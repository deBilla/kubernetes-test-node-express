{{- if .Values.orderService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ecommerce.fullname" . }}-order-service
  labels:
    {{- include "ecommerce.labels" . | nindent 4 }}
    app.kubernetes.io/name: order-service
spec:
  replicas: {{ .Values.orderService.replicaCount }}
  selector:
    matchLabels:
      {{- include "ecommerce.selectorLabels" (dict "name" "order-service" "release" .Release.Name) | nindent 6 }}
  template:
    metadata:
      {{- if .Values.linkerd.enabled }}
      annotations:
        linkerd.io/inject: enabled
        config.linkerd.io/skip-inbound-ports: "{{ $.Values.metricsPort }}"
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ $.Values.metricsPort }}"
        prometheus.io/path: "/metrics"
      {{- end }}
      labels:
        {{- include "ecommerce.selectorLabels" (dict "name" "order-service" "release" .Release.Name) | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: order-service
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
          image: "{{ .Values.orderService.image.repository }}:{{ .Values.orderService.image.tag }}"
          imagePullPolicy: {{ .Values.orderService.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.orderService.containerPort }}
              protocol: TCP
            - containerPort: {{ .Values.metricsPort }}
              protocol: TCP
              name: metrics
          env:
            - name: PORT
              value: {{ .Values.orderService.containerPort | quote }}
            - name: OTEL_SERVICE_NAME
              value: "order-service"
            {{- if .Values.mongodb.uri }}
            - name: DB_URI
              valueFrom:
                configMapKeyRef:
                  name: {{ include "ecommerce.fullname" . }}-config
                  key: DB_URI
            {{- end }}
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "ecommerce.fullname" . }}-mongodb
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "ecommerce.fullname" . }}-mongodb
                  key: DB_PASSWORD
            - name: DB_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "ecommerce.fullname" . }}-mongodb
                  key: DB_URL
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.orderService.containerPort }}
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.orderService.containerPort }}
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.orderService.resources | nindent 12 }}
{{- end }}
